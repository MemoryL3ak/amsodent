node_modules
dist
.DS_Store
.env
.env.*
!.env.example

certificates

# Artefactos generados por scripts (SQLs producidos a partir de CSVs locales)
scripts/output/

# Cache local de Claude Code y build de Next.js
.claude/
.next/

# Clave de cuenta de servicio de Google (SECRETA — nunca subir al repo)
backend/google-sa.json
**/google-sa.json
**/*service-account*.json
# Export de facturas con datos reales — nunca commitear
facturas_duplicadas.csv
